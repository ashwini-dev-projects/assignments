Ideyaslabs Interview


1)Write a program to find the each character and its count of appearance

input: Name = "Ashwini"

method 1: data = {i:Name.count(i) for i in Name}
	  print(data)

method 2: without using any built in methods only use if condition and for loop
	  seen = {}

          for i in Name:
		if i in seen:
		   seen[i] += 1
		else:
		   seen[i] = 1
          print(seen)


2) from the given sql Table to get employ id, employ name, manager name

🧾 Sample Data (Employees table):
employee_id	employee_name	manager_id
1		Alice		NULL
2		Bob		1
3		Charlie		1
4		David		2
5		Eva		2

Example:

SELECT 

e.employee_id,
e.employee_name,
e.manager_id,
m.manager_id AS manager_name

FROM Employees as e

LEFT JOIN Employees as m

ON e.manager_id = m.employee_id


Output:
employee_id	employee_name	manager_id	manager_name
1		Alice		NULL		NULL
2		Bob		1		Alice
3		Charlie		1		Alice
4		David		2		Bob
5		Eva		2		Bob


3) How to convert JSON data into Table data using Python

Ans: using pandas

import pandas as pd

# Sample JSON data
json_data = [
    {"employee_id": 1, "employee_name": "Alice", "manager_id": None},
    {"employee_id": 2, "employee_name": "Bob", "manager_id": 1},
    {"employee_id": 3, "employee_name": "Charlie", "manager_id": 1},
    {"employee_id": 4, "employee_name": "David", "manager_id": 2},
    {"employee_id": 5, "employee_name": "Eva", "manager_id": 2}
]

# Convert to DataFrame (table format)
df = pd.DataFrame(json_data)

# Display the table
print(df)


4) How to extract Data from external API and API link has given to extract data

ANS: using requests Library


5) How to improve the API performance and what are the key features developer should check

6) what are built in data types and primitive data types

7) is List and Integer are classes

8) what is the use of decorators

9) How to improve the query optimization

10) Impprove high latency to low latency of api resposne


